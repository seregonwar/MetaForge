const vscode = require('vscode');

function activate(context) {
    console.log('MetaForge Syntax Highlighting Activated');
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
};
